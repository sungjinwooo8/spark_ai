{
  "name": "ai-chatbot",
  "version": "1.0.0",
  "description": "Unified package.json for deploying the AI Chatbot frontend and backend together",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm install && npm run build && cd ../server && npm install"
  }
}
